export interface IProduct {
  image?: string;
  title?: string;
  price?: number;
  quantity?: number;
  category?: string;
  description?: string;
}

export interface IProductItem {
  title: string;
  price: number;
  quantity: number;
}
